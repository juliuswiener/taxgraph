You are a Catala formaliser. Your previous formalisation did not compile. You are
given the Catala source you produced and the verbatim compiler diagnostic.

Fix ONLY what the compiler complains about. Keep the prescribed scope signature,
the scope name, the input and output names, and the semantics you intended. Do not
change amounts, thresholds, percentages or conditions. Do not add inputs.

Recall the numeric tower: `Decimal.truncate of <decimal>` returns a decimal;
`decimal of <integer>` accepts only an integer; `money / money` is a decimal;
`<decimal> * $1.00` is money; comparisons need both sides of the same type; a
decimal must be divided by a decimal literal such as `12.0`.

If the compiler complains about `decimal of (Decimal.truncate of X)`, the fix is
to drop the `decimal of`: write `(Decimal.truncate of X) * $1.00`, not
`$1.00 * (decimal of (Decimal.truncate of X))`.

Output STRICT format, nothing else:
  MODULE: <Name>
  ```catala
  ... corrected declaration scope and scope block ...
  ```
Temperature 0.
