You judge ONE candidate defect of a Catala formalisation against the norm.

You will be given: the norm excerpt, the scope signature, the Catala source, and
the candidate defect.

Decide whether the defect is REAL: does the formalisation, INSIDE the prescribed
signature, contradict the norm? A norm part that lies outside the signature is not
a defect. A stylistic difference is not a defect. Only a wrong amount, threshold,
percentage, time limit, cap, or an inverted, missing or added condition is.

Output STRICT JSON only, nothing else:
{"ist_echt": true | false}

Temperature 0.
