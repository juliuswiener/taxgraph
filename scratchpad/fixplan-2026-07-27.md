# Fixplan 2026-07-27 — Instructor-Spec (Devs folgen exakt)

Reviewed by Instructor am Code verifiziert. Werte/Units unten sind BINDEND.
Nie Gesetzeswert aus Gedächtnis ändern — Quelle: sources/gesetze-im-internet/estg_p35a_2026-07-09.txt.

Gate-Prinzip: deterministisch (pytest exit 0). Self-green misstrauen — Instructor
verifiziert per git + eigenem Lauf.

---

## BUG 1 — steuerermaessigungen_cent Typo (P0, 1 Zeichen)

Datei: `produkt/haut/api.py`, Zeile 1004.

```
- g["steuerermaessigungen_cent"] = base // 2 if mitver else base
+ g["steuerermaessigungen"]      = base // 2 if mitver else base
```

Ursache: `:1018` macht `g["steuerermaessigungen"] += ...` → KeyError weil Key als
`_cent` geschrieben. Rentner-Pfad (:1532) hat den Bug NICHT — nur gesamt.

Gate 1: `python -m pytest tests/test_solz_ring.py -q` → 3 passed, 0 failed.
Erwartet fällt Gesamt-Suite von 124 → 21 rot.

---

## BUG 2 — §35a-haushaltsnahe totes Wiring + Unit + Gating (P0, Option A)

### Gesetzeslage (estg_p35a_2026-07-09.txt, verifiziert)
- Abs.1 Minijob (max 510€), Abs.2 Dienstleistung (max 4000€), Abs.3 Handwerker (max 1200€), je 20%.
- Abs.4 S.1: Haushalt in EU/EWR — Voraussetzung für Abs.1–3 (ALLE).
- Abs.5 S.3: Rechnung + unbare Zahlung — Voraussetzung NUR für Abs.2/3, NICHT Minijob.
- "auf Antrag" in Abs.1/2/3.

### Instructor-Design-Entscheidungen (Julius kann vetoen)
1. **Antrag = implizit** durch Eingabe der Aufwendungen. Flag `antrag_ermaessigter_satz`
   NICHT als §35a-Gate nutzen (gehört §34/§16 Tarifermäßigung — Überladung vermeiden).
   → Accessor: antrag-Gate für §35a ENTFERNEN.
2. **EU/EWR = Pflicht-Kegel-Feld** `hh_in_eu_ewr`, askable in haushalt-Scheibe → immer
   beantwortet (Projekt-Linie „Pflicht-Kegel"), kein stiller 0 und kein stiller Grant.
3. **rechnung/unbar** gatet nur Abs.2/3.
4. **Unit-Contract: Accessor rechnet EURO rein, EURO raus.** Caps in EURO: 510/4000/1200
   (KEIN *100). Grund: der `steuerermaessigungen`-Slot ist EURO (p35c_gesamt_deckel wird
   dazu addiert und ist euro-nativ). api reicht `_c(...) // 100` (Cent→Euro) in BEIDEN Pfaden.
   Test-Seeds in EURO (510, 400, 900, 1200, 600, 1710, 0).
   [KORREKTUR 2026-07-27: ursprünglich CENT gespect — falsch, 100×-Bug. EURO ist korrekt.]

### Fix A — Accessor `golden/runner.py` (def catala_p35a_haushaltsnahe, ab Z.312)
Neuer Kontrakt (CENT, hh_-Keys, Gate per-Absatz):

```python
def catala_p35a_haushaltsnahe(s: dict) -> int:
    """§ 35a Abs. 1-5 EStG. CENT rein, CENT raus. Antrag implizit (Aufwand-Eingabe).
    EU/EWR (Abs.4) gatet Abs.1-3. Rechnung+unbar (Abs.5 S.3) gatet NUR Abs.2/3."""
    minijob = int(s.get("hh_minijob_aufwendungen", 0))
    dienstleistungen = int(s.get("hh_dienstleistungen", 0))
    handwerker = int(s.get("hh_handwerker_arbeitskosten", 0))
    eu_ewr = s.get("hh_in_eu_ewr", {}).get("wert") is True
    rechnung = s.get("hh_rechnung_unbar", {}).get("wert") is True
    if not eu_ewr:                       # Abs.4: gilt für alle drei
        return 0
    if not rechnung:                     # Abs.5 S.3: nur Abs.2/3, Minijob unberührt
        dienstleistungen = 0
        handwerker = 0
    betrag = minijob + dienstleistungen + handwerker
    if betrag == 0:
        return 0
    ermessigung = 0
    if minijob > 0:
        ermessigung += min(minijob * 20 // 100, 510 * 100)
    if dienstleistungen > 0:
        ermessigung += min(dienstleistungen * 20 // 100, 4000 * 100)
    if handwerker > 0:
        ermessigung += min(handwerker * 20 // 100, 1200 * 100)
    if s.get("p35a_mitveranlagung", {}).get("wert") is True:
        ermessigung = ermessigung // 2
    return ermessigung
```
HINWEIS: getrennte Deckel PRO Topf (nicht Summe gegen Summen-Deckel) — bisheriger Code
addierte Deckel und deckelte die Gesamt-20%, das ist bei gemischten Töpfen falsch.

### Fix B — api.py gesamt-Pfad (Z.998-1002): hh_-Keys + Flags, CENT, kein abs23_aus
```python
            base = runner.catala_p35a_haushaltsnahe({
                "hh_minijob_aufwendungen": _c("hh_minijob_aufwendungen"),
                "hh_dienstleistungen": _c("hh_dienstleistungen"),
                "hh_handwerker_arbeitskosten": _c("hh_handwerker_arbeitskosten"),
                "hh_in_eu_ewr": f.get("hh_in_eu_ewr", {}),
                "hh_rechnung_unbar": f.get("hh_rechnung_unbar", {}),
                "p35a_mitveranlagung": f.get("p35a_mitveranlagung", {})})
            g["steuerermaessigungen"] = base
```
(die `abs23_aus`-Zeile 998 und die `mitver`-Halbierung Z.1003-1004 entfallen — Accessor macht beides.)

### Fix C — api.py rentner-Pfad (Z.1526-1532): analog, KEIN //100 mehr
```python
            base = runner.catala_p35a_haushaltsnahe({
                "hh_minijob_aufwendungen": _c("hh_minijob_aufwendungen"),
                "hh_dienstleistungen": _c("hh_dienstleistungen"),
                "hh_handwerker_arbeitskosten": _c("hh_handwerker_arbeitskosten"),
                "hh_in_eu_ewr": f.get("hh_in_eu_ewr", {}),
                "hh_rechnung_unbar": f.get("hh_rechnung_unbar", {}),
                "p35a_mitveranlagung": f.get("p35a_mitveranlagung", {})})
            rentner_g["steuerermaessigungen"] = base
```
Unit-Bug war: Rentner sendete `_c(...) // 100` (EUR) an CENT-Accessor → 100x zu klein.

### Fix D — Erreichbarkeit: hh_in_eu_ewr als Pflicht-Kegel in haushalt-Scheibe
- api.py Z.204-205: `HAUSHALT_35A` um `hh_in_eu_ewr` erweitern (Pflicht-Kegel wie hh_rechnung_unbar).
- Sicherstellen dass hh_in_eu_ewr in der/den Scheibe(n) SCHEIBEN.felder steht die gesamt+rentner nutzen.
- bindung: `hh_in_eu_ewr` existiert in bindung_sonder_agb_35a.yaml:204 — prüfen dass verified/askable.
- Gate D: `python -m pytest tests/test_erreichbarkeit_gate.py -q` grün.

### Fix E — Test-Seeds auf CENT (tests/test_haushalt_35a_10b.py, ab Z.28)
Accessor ist CENT — Seeds+Erwartung in CENT, Flags mitgeben. Neue Parametrisierung:
```python
GATE = {"hh_in_eu_ewr": {"wert": True}, "hh_rechnung_unbar": {"wert": True}}
@pytest.mark.parametrize("s,erwartet", [
    ({"hh_minijob_aufwendungen": 280000},                     51000),   # 2800€: 20%=560€>510€ Deckel
    ({"hh_minijob_aufwendungen": 200000},                     40000),   # 2000€: 20%=400€<=510€
    ({"hh_handwerker_arbeitskosten": 450000},                 90000),   # 4500€: 20%=900€<=1200€
    ({"hh_handwerker_arbeitskosten": 1000000},               120000),   # 10000€: 20%=2000€>1200€ Deckel
    ({"hh_dienstleistungen": 300000},                         60000),   # 3000€: 20%=600€<=4000€
    ({"hh_minijob_aufwendungen": 280000, "hh_handwerker_arbeitskosten": 1000000}, 171000),  # 51000+120000
    ({},                                                          0),
])
def test_p35a_seeds(R, s, erwartet):
    assert R.catala_p35a_haushaltsnahe({**GATE, **s}) == erwartet
```
Minijob braucht KEIN hh_rechnung_unbar → separater Test:
```python
def test_p35a_minijob_ohne_rechnung(R):
    assert R.catala_p35a_haushaltsnahe(
        {"hh_in_eu_ewr": {"wert": True}, "hh_minijob_aufwendungen": 280000}) == 51000
def test_p35a_handwerker_ohne_rechnung_null(R):
    assert R.catala_p35a_haushaltsnahe(
        {"hh_in_eu_ewr": {"wert": True}, "hh_handwerker_arbeitskosten": 450000}) == 0
def test_p35a_ohne_eu_ewr_null(R):
    assert R.catala_p35a_haushaltsnahe(
        {"hh_handwerker_arbeitskosten": 450000, "hh_rechnung_unbar": {"wert": True}}) == 0
```
Fixture R (Z.20) auch anpassen: `runner.catala_p35a_haushaltsnahe({"hh_in_eu_ewr":{"wert":True}})`.

### Fix F — Ring-Diff-Test (PFLICHT, beweist Feuern durch die API)
In tests/test_ring_regression_kampagne.py bzw. test_paket_b_e2e_http.py: Fall mit
Handwerker + hh_in_eu_ewr=true + hh_rechnung_unbar=true → festzusetzende ESt sinkt um
den §35a-Betrag vs. gleicher Fall ohne §35a. Und mitver-Test (test_p35a_mitveranlagung_
senkt_steuer_gesamt) muss grün werden.

Gate 2 (gesamt): `python -m pytest tests/test_haushalt_35a_10b.py tests/test_p35a_accessor.py tests/test_ring_regression_kampagne.py tests/test_p35c_ring.py tests/test_p10_1a_ring.py tests/test_p10_1_5_ring.py -q` → 0 failed.

---

## BUG 3 — untracked hard-deps + auth-Tests (Integrität)

server.py (tracked, Z.30-31) importiert `auth`+`audit` → beide untracked → HEAD startet
nicht auf clean clone. 2101 LOC untracked: produkt/auth/, produkt/store/{audit,file_backend,
sql_backend,__init__,migrations}, produkt/konsistenz/{check_pauschalen,preflight},
produkt/mapping/fragekatalog.

Schritte:
1. `_req`-Helper-Bug fixen: tests/test_auth_integration.py (+comprehensive/security) nutzen
   `_req` ohne Definition (NameError, 8 rot). Andere Ring-Tests definieren lokales `_req`
   (siehe tests/test_solz_ring.py:40). Denselben Helper in die auth-Tests einfügen (oder
   in ein conftest.py hochziehen und überall importieren — sauberer).
2. Untracked Module kurz reviewen (Import-sauber, keine offensichtlichen Platzhalter).
3. `git add` der untracked Module + Tests, EIN Commit. KEIN push (Julius-Cap).
Gate 3: `python -m pytest tests/test_auth_integration.py tests/test_auth_comprehensive.py
tests/test_auth_security.py -q` → 0 failed; `git status --porcelain produkt/ tests/` zeigt
keine ?? mehr für die genannten Pfade.

---

## FINALE — nach Bug 1+2+3
`python -m pytest tests/ -q` → **0 failed** (Ziel: grüne Suite als Abnahmegate).
Instructor verifiziert selbst. Danach erst File-Split (Task 4).
